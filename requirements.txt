# Discord Bot Dependencies
discord.py>=2.3.0
aiofiles>=23.2.0
aiosqlite>=0.19.0
python-dotenv>=1.0.0
pillow>=10.0.0
asyncio-throttle>=1.0.2
colorama>=0.4.6
rich>=13.0.0
aiohttp>=3.9.0

# Database (for Railway PostgreSQL if needed in future)
psycopg2-binary>=2.9.0

# Data Processing
pandas>=2.0
jsonschema>=4.0

# Local Sync API (Flask - for local server only, not needed on Railway)
flask>=2.3.0
flask-cors>=4.0.0
werkzeug>=2.3.0
