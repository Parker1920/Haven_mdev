# Round Table AI Dependencies

# Cloud AI Providers
anthropic>=0.18.0  # Claude AI
openai>=1.12.0     # GPT-4 and GPT-3.5

# Core dependencies (should already be installed with Haven-UI)
fastapi>=0.109.0
aiosqlite>=0.19.0
